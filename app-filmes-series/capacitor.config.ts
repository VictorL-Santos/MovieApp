import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppFilmes/Series',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
